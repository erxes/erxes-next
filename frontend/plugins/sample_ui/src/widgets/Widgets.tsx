const Widgets = ({
  contentType,
  contentId,
  message,
}: {
  contentType: string;
  contentId: string;
  message: string;
}) => {
  return <div>Widgets sample {message}</div>;
};

export default Widgets;
