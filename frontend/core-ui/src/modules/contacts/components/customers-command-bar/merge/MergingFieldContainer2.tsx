import { FieldType } from '@/contacts/components/customers-command-bar/merge/MergeMap';
import { ChoiceboxGroup } from 'erxes-ui/components';

interface MergingFieldContainerProps {
  fieldName: any;
  fieldValue: [any, any];
  onClick: () => void;
  type?: FieldType;
}

export const MergingFieldContainer = () => {
  return <div></div>;
};
