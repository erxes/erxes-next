export const commonParams = `
    ids: [String]
    excludeIds: Boolean
    perPage: Int
    page: Int
    searchValue: String,
    status: String,
    onlyFirstLevel: Boolean,
    parentId: String
`;
