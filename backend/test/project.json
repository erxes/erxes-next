{
  "name": "test",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "backend/test/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "cwd": "backend/test",
        "commands": ["pnpm build"]
      },
      "dependsOn": ["^build", "build:packageJson"]
    },

    "build:packageJson": {
      "executor": "@nx/js:tsc",
      "options": {
        "main": "backend/test/dist/src/main.js",
        "tsConfig": "backend/test/tsconfig.json",
        "outputPath": "backend/test/dist",
        "updateBuildableProjectDepsInPackageJson": true,

        "buildableProjectDepsInPackageJsonType": "dependencies"
      }
    },

    "start": {
      "executor": "nx:run-commands",
      "dependsOn": ["typecheck", "build"],
      "options": {
        "cwd": "backend/test",
        "command": "NODE_ENV=development && node dist/src/main.js"
      }
    },

    "serve": {
      "executor": "nx:run-commands",

      "options": {
        "cwd": "backend/test",
        "command": "pnpm dev"
      }
    },

    "docker-build": {
      "dependsOn": ["build"],
      "command": "docker build -f backend/test/Dockerfile . -t test"
    }
  }
}
